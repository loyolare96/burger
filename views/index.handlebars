<h1>Boigahs</h1>

<h2>Eat-Da-Burger!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgah">Burger Name:</label>
    <input type="text" id="burgah" name="name">
  </div>

  <button type="submit">Add burger</button>
</form>
